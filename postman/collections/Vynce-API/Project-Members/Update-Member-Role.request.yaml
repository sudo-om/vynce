$kind: http-request
name: Update Member Role
method: PATCH
url: '{{baseUrl}}/api/projects/{{projectId}}/members/{{memberId}}'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "role": "VIEWER"
    }
